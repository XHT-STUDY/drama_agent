/**
 * A-01 占位测试 — 验证前端测试框架可运行。
 *
 * 后续阶段添加真实测试后，可删除本文件。
 */
import { describe, it, expect } from "vitest";

describe("frontend test framework", () => {
  it("should always pass", () => {
    expect(true).toBe(true);
  });
});
